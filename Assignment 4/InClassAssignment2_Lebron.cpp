#include <iostream>
using namespace std;

int main() {
    // Print first 10 even positive integers in descending order
    for (int num = 20; num > 0; num -= 2) {
        cout << num << " ";
    }
    cout << endl;
    return 0;
}